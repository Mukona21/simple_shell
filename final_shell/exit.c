#include "main.h"

/**
 * exit_shell - Exit the shell with a given status code
 *
 * @status_code: The exit status code to use
 */

void exit_shell(int status_code)
{
	/* Perform any necessary cleanup before exiting */

	exit(status_code);
}
